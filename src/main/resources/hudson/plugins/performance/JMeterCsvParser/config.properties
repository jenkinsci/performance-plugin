csv.pattern=CSV Pattern
csv.pattern.delimiter=Delimiter
csv.pattern.delimiter.default=,
csv.pattern.default=timestamp,elapsed,responseCode,threadName,success,failureMessage,grpThreads,allThreads,URL,Latency,SampleCount,ErrorCount
csv.pattern.description=comma separated list of the titles. The parser will only use (and they are also *required*) timestamp, elapsed,responseCode, success and URL
csv.skipFirstLine=Skip First line
csv.skipFirstLine.description=Tell's the parser weather it should skip the first line from it's processing (skip it if the first line is the header)